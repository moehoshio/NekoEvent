@PACKAGE_INIT@

# Find required dependencies
# Note: find_dependency() must not be given REQUIRED or QUIET; it forwards the
# REQUIRED/QUIET-ness requested by the caller of find_package(NekoEvent).
include(CMakeFindDependencyMacro)
find_dependency(NekoSchema)

# Include the targets file
include("${CMAKE_CURRENT_LIST_DIR}/NekoEventTargets.cmake")

# Recreate alias for consumers
if(TARGET NekoEvent AND NOT TARGET Neko::Event)
	add_library(Neko::Event ALIAS NekoEvent)
endif()
if(TARGET NekoEvent_module AND NOT TARGET Neko::Event::Module)
	add_library(Neko::Event::Module ALIAS NekoEvent_module)
endif()

check_required_components(NekoEvent)